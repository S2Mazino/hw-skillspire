function lengthCompare(){
    var vege = "Avocado"
    var fruit = "Watermelon"
  if(vege.length > fruit.length){
    console.log("The string '"+ vege + "' is longer than the string '" + fruit+"'")
  } else if(vege.length < fruit.length){
    console.log("The string '"+ fruit + "' is longer than the string '" + vege+"'")
  } else{
  }
  }
  lengthCompare()